module.exports = {
  plugins: ["tailwindcss", "postcss-preset-env"],
};

// import tailwind css that we installed by npm,
// invoke it with tailwind.config.js, and
// invoke postcss-preset-env module WHICH HELPS browsers process our CSS, including autoprefixer
